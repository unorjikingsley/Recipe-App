databases:
  - name: recipe
    databaseName: recipe_jk74
    user: recipe_jk74_user

services:
  - type: web
    name: recipe-webapp
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: recipe
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
